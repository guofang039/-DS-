# 小翔DS 远程桌面 - 多用户功能说明

## 一、RustDesk 内置多用户功能

RustDesk **本身已内置完整的多用户管理系统**，通过 **API Server** 实现。

### 核心功能

| 功能 | 说明 |
|------|------|
| **用户登录** | 用户名/密码登录，获取 access_token |
| **地址簿** | 每个用户独立的设备地址簿 |
| **标签分组** | 对设备进行标签分类管理 |
| **设备组** | 按部门/团队分组管理设备 |
| **策略管理** | 不同用户组应用不同策略 |
| **审计日志** | 连接、文件传输等操作记录 |

### 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    RustDesk Server Pro                       │
│  ┌─────────┐  ┌─────────┐  ┌─────────────────────────────┐ │
│  │  hbbs   │  │  hbbr   │  │      Web API Server         │ │
│  │(信令服务)│  │(中继服务)│  │  :21114 (管理后台)           │ │
│  └─────────┘  └─────────┘  │  /api/users    - 用户管理    │ │
│                            │  /api/ab       - 地址簿      │ │
│                            │  /api/devices  - 设备管理    │ │
│                            │  /api/audit    - 审计日志    │ │
│                            └─────────────────────────────┘ │
│                                                              │
│  端口:                                                       │
│  21114 - Web API / 管理后台                                  │
│  21115 - NAT 测试                                            │
│  21116 - 信令服务 (TCP/UDP)                                  │
│  21117 - 中继服务                                            │
│  21118 - WebSocket 信令                                      │
│  21119 - WebSocket 中继                                      │
└─────────────────────────────────────────────────────────────┘
```

## 二、多用户功能详解

### 2.1 用户认证流程

```
┌──────────┐     POST /api/login      ┌──────────┐
│  客户端   │ ───────────────────────► │ API Server│
│          │  {username, password}    │          │
│          │                          │          │
│          │ ◄─────────────────────── │          │
│          │  {access_token, user_id} │          │
└──────────┘                          └──────────┘
     │
     │  存储 access_token 到本地配置
     ▼
┌──────────┐
│ 后续请求  │  Header: Authorization: Bearer <token>
│ 自动携带  │
└──────────┘
```

### 2.2 地址簿功能

每个用户拥有独立的地址簿，可以：

- **添加设备** - 通过 ID 添加远程设备
- **设置别名** - 为设备设置易记的名称
- **标签分组** - 用标签对设备分类（如：服务器、办公电脑、测试机）
- **设置密码** - 为每个设备单独保存密码
- **添加备注** - 记录设备用途等信息

```javascript
// src/ui/ab.tis - 地址簿数据结构
var ab = { 
    tags: ["服务器", "办公", "测试"],  // 标签列表
    peers: [                          // 设备列表
        {
            id: "123456789",
            alias: "生产服务器",
            tags: ["服务器"],
            password: "encrypted...",
            note: "主要业务服务器"
        }
    ]
};
```

### 2.3 设备组管理

管理员可以将设备分配到不同的组：

```bash
# 命令行管理
rustdesk --device_group_name "运维组"
rustdesk --device_username "张三"
rustdesk --device_name "张三-办公电脑"
```

### 2.4 策略管理

不同用户组可以应用不同的安全策略：

| 策略选项 | 说明 |
|----------|------|
| 允许远程控制 | 是否允许被远程控制 |
| 允许文件传输 | 是否允许文件传输 |
| 允许剪贴板同步 | 是否允许剪贴板共享 |
| 允许键盘输入 | 是否允许键盘操作 |
| 允许鼠标输入 | 是否允许鼠标操作 |
| 隐私模式 | 是否允许隐私模式 |
| 强制确认连接 | 是否必须手动确认连接 |

## 三、部署配置

### 3.1 服务端部署

**Docker 一键部署：**

```bash
docker run -d \
  --name rustdesk-server \
  -p 21114:21114 \
  -p 21115:21115 \
  -p 21116:21116 \
  -p 21116:21116/udp \
  -p 21117:21117 \
  -p 21118:21118 \
  -p 21119:21119 \
  -v /opt/rustdesk:/root \
  -e "RELAY=your-server.com:21117" \
  -e "ENCRYPTED_ONLY=1" \
  rustdesk/rustdesk-server-pro:latest
```

**Docker Compose 部署：**

```yaml
version: '3'
services:
  rustdesk-server:
    image: rustdesk/rustdesk-server-pro:latest
    container_name: rustdesk-server
    ports:
      - "21114:21114"
      - "21115:21115"
      - "21116:21116"
      - "21116:21116/udp"
      - "21117:21117"
      - "21118:21118"
      - "21119:21119"
    volumes:
      - ./data:/root
    environment:
      - RELAY=rustdesk.example.com:21117
      - ENCRYPTED_ONLY=1
    restart: unless-stopped
```

### 3.2 客户端配置

**方式一：通过界面配置**

1. 打开 RustDesk
2. 设置 → 网络 → ID/中继服务器
3. 输入服务器地址：`your-server.com:21116`
4. 设置 → 安全 → 设置永久密码

**方式二：命令行配置**

```bash
# 设置服务器地址
rustdesk --server your-server.com

# 设置 API 服务器
rustdesk --api-server https://your-server.com

# 登录用户
rustdesk --user_name "张三"
```

**方式三：配置文件**

```toml
# ~/.config/rustdesk/RustDesk2.toml
[options]
custom-rendezvous-server = "your-server.com:21116"
relay-server = "your-server.com:21117"
api-server = "https://your-server.com"
```

### 3.3 获取服务器公钥

```bash
# 从服务器获取公钥
docker exec rustdesk-server cat /root/id_ed25519.pub

# 客户端设置公钥
# 设置 → 网络 → Key → 粘贴公钥
```

## 四、管理后台使用

### 4.1 访问管理后台

```
https://your-server.com:21114
```

### 4.2 用户管理

| 操作 | 说明 |
|------|------|
| 添加用户 | 创建新用户账号 |
| 用户组 | 将用户分配到组 |
| 权限设置 | 设置用户的操作权限 |
| 策略绑定 | 为用户/组绑定安全策略 |

### 4.3 设备管理

| 操作 | 说明 |
|------|------|
| 设备列表 | 查看所有注册设备 |
| 设备分组 | 将设备分配到组 |
| 在线状态 | 查看设备在线情况 |
| 设备注册 | 删除/禁用设备 |

### 4.4 审计日志

| 日志类型 | 内容 |
|----------|------|
| 连接日志 | 谁在何时连接了哪台设备 |
| 文件传输 | 文件传输记录 |
| 操作日志 | 用户操作记录 |

## 五、CLI 管理命令

RustDesk 提供完整的命令行管理接口：

```bash
# 用户管理
rustdesk --user_name "张三" --token <admin_token>

# 设备组管理
rustdesk --device_group_name "运维组" --token <admin_token>

# 设备管理
rustdesk --device_username "张三" --device_name "办公电脑" --token <admin_token>

# 地址簿管理
rustdesk --address_book_name "我的设备" \
         --address_book_tag "服务器" \
         --address_book_alias "生产服务器" \
         --address_book_password "xxx" \
         --address_book_note "主要业务服务器" \
         --token <admin_token>

# 策略管理
rustdesk --strategy_name "运维策略" --token <admin_token>
```

## 六、安全配置

### 6.1 强制加密

```bash
# 服务端强制加密连接
ENCRYPTED_ONLY=1
```

### 6.2 密码策略

- **永久密码** - 管理员设置，长期有效
- **临时密码** - 每次启动自动生成
- **确认连接** - 每次连接需手动确认

### 6.3 网络安全

```bash
# 限制访问 IP
iptables -A INPUT -p tcp --dport 21114 -s 10.0.0.0/8 -j ACCEPT
iptables -A INPUT -p tcp --dport 21114 -j DROP

# 启用 HTTPS（推荐使用 Nginx 反向代理）
```

### 6.4 Nginx 反向代理配置

```nginx
server {
    listen 443 ssl;
    server_name rustdesk.example.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # API 服务
    location /api/ {
        proxy_pass http://127.0.0.1:21114;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket
    location /ws/ {
        proxy_pass http://127.0.0.1:21118;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 七、集成扩展

### 7.1 LDAP/AD 集成

RustDesk Server Pro 支持 LDAP/AD 用户认证：

```
用户登录 → LDAP 认证 → 获取用户信息 → 创建/更新本地用户
```

### 7.2 SSO 单点登录

支持 OAuth2、SAML 等单点登录协议。

### 7.3 API 接口

```bash
# 用户登录
POST /api/login
{
    "username": "zhangsan",
    "password": "xxx"
}

# 获取地址簿
GET /api/ab/get
Header: Authorization: Bearer <token>

# 更新地址簿
POST /api/ab
{
    "tags": ["服务器", "办公"],
    "peers": [...]
}

# 获取设备列表
GET /api/devices
Header: Authorization: Bearer <token>

# 审计日志
GET /api/audit/logs?type=conn&start=2024-01-01&end=2024-12-31
```

## 八、快速开始

### 完整部署流程

```bash
# 1. 部署服务器
docker run -d --name rustdesk-server \
  -p 21114-21119:21114-21119 \
  -p 21116:21116/udp \
  -v $PWD/data:/root \
  rustdesk/rustdesk-server-pro:latest

# 2. 获取公钥
docker exec rustdesk-server cat /root/id_ed25519.pub

# 3. 访问管理后台创建用户
# https://your-server.com:21114

# 4. 客户端配置服务器地址和公钥

# 5. 用户登录后即可使用地址簿功能
```

## 九、开源版 vs Pro 版对比

| 功能 | 开源版 | Pro 版 |
|------|:------:|:------:|
| 基础远程控制 | ✅ | ✅ |
| 文件传输 | ✅ | ✅ |
| 端口转发 | ✅ | ✅ |
| 用户管理 | ❌ | ✅ |
| 地址簿同步 | ❌ | ✅ |
| 设备分组 | ❌ | ✅ |
| 策略管理 | ❌ | ✅ |
| 审计日志 | ❌ | ✅ |
| LDAP/SSO | ❌ | ✅ |
| Web 管理后台 | ❌ | ✅ |

> **注意**：开源版客户端支持多用户功能，但需要配合 Pro 版服务器使用。

---

> 📖 仓库地址: https://gitee.com/xueguofang/xiaoxiangyun-ds-remote
> 
> 📝 版本: v1.4.6
> 
> 👤 维护: xueguofang
