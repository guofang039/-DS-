# 小翔DS 远程桌面多用户方案

## 一、系统架构概述

RustDesk 采用 **客户端-服务器** 架构，核心组件如下：

```
┌─────────────┐     ┌─────────────────────────────────┐     ┌─────────────┐
│  客户端 A    │◄───►│          服务端 (自建)            │◄───►│  客户端 B    │
│  (控制端)    │     │                                 │     │  (被控端)    │
└─────────────┘     │  ┌───────────┐  ┌───────────┐  │     └─────────────┘
                    │  │   hbbs    │  │   hbbr     │  │
                    │  │ (信令服务) │  │ (中继服务) │  │
                    │  └───────────┘  └───────────┘  │
                    │                                 │
                    │  端口:                          │
                    │  21116 - NAT 打洞/信令          │
                    │  21117 - 中继转发               │
                    │  21118 - WebSocket 信令         │
                    │  21119 - WebSocket 中继         │
                    └─────────────────────────────────┘
```

### 核心组件说明

| 组件 | 说明 |
|------|------|
| **hbbs** | RustDesk 信令/注册服务器，负责设备注册、ID 分配、NAT 打洞协调 |
| **hbbr** | RustDesk 中继服务器，当 P2P 直连失败时转发流量 |
| **客户端** | 包含控制端和被控端功能，通过 ID + 密码建立连接 |

### 连接流程

```
1. 被控端启动 → 向 hbbs 注册，获取唯一 ID
2. 控制端输入 ID → hbbs 协助 NAT 打洞
3. 打洞成功 → 建立 P2P 直连（加密传输）
4. 打洞失败 → 通过 hbbr 中继转发
5. 连接建立 → 输入密码验证 → 远程控制
```

## 二、当前单用户模式分析

### ID 生成机制

RustDesk 客户端 ID 基于 **MAC 地址** 自动生成：

```rust
// libs/hbb_common/src/config.rs
fn get_auto_id() -> Option<String> {
    let mut id = 0u32;
    if let Ok(Some(ma)) = mac_address::get_mac_address() {
        for x in &ma.bytes()[2..] {
            id = (id << 8) | (*x as u32);
        }
        id &= 0x1FFFFFFF;  // 9位数字ID
        Some(id.to_string())
    } else {
        None
    }
}
```

- 每台设备自动生成一个 9 位数字 ID
- ID 与网卡 MAC 地址绑定，全局唯一
- 同一台机器只有一个 ID

### 认证机制

```
┌──────────┐    密码验证     ┌──────────┐
│ 控制端    │ ──────────────► │ 被控端    │
│          │  永久密码/随机密码│          │
└──────────┘                 └──────────┘

密码类型:
- 永久密码: 手动设置，存储在本地配置
- 随机密码: 每次启动自动生成，可自定义长度
- 确认连接: 弹窗确认，无需密码
```

### 配置文件结构

```
~/.config/rustdesk/
├── RustDesk.toml          # 主配置 (ID, 密钥对, 密码)
├── RustDesk2.toml         # 扩展配置 (服务器地址, 选项)
└── .local/config/         # 本地配置
```

## 三、多用户方案设计

### 方案一：自建服务器 + 用户组管理（推荐）

#### 架构图

```
┌──────────────────────────────────────────────────┐
│                 管理后台 (Web)                     │
│         用户管理 / 权限控制 / 审计日志              │
└───────────────────┬──────────────────────────────┘
                    │
┌───────────────────▼──────────────────────────────┐
│              RustDesk Server Pro                  │
│  ┌─────────┐  ┌─────────┐  ┌──────────────────┐ │
│  │  hbbs   │  │  hbbr   │  │  用户认证模块     │ │
│  │(信令服务)│  │(中继服务)│  │ (LDAP/SSO/本地)  │ │
│  └─────────┘  └─────────┘  └──────────────────┘ │
│                                                   │
│  端口: 21114(Web) 21116(信令) 21117(中继)         │
└──────────────────────────────────────────────────┘
        │              │              │
   ┌────▼────┐   ┌────▼────┐   ┌────▼────┐
   │ 用户组A  │   │ 用户组B  │   │ 用户组C  │
   │ 运维团队 │   │ 开发团队 │   │ 管理团队 │
   └─────────┘   └─────────┘   └─────────┘
```

#### 部署步骤

**1. 部署 RustDesk Server Pro**

```bash
# Docker 一键部署
docker run -d \
  --name rustdesk-server \
  -p 21114:21114 \
  -p 21115:21115 \
  -p 21116:21116 \
  -p 21116:21116/udp \
  -p 21117:21117 \
  -p 21118:21118 \
  -p 21119:21119 \
  -v /opt/rustdesk-server:/root \
  rustdesk/rustdesk-server-pro:latest
```

**2. 配置用户组**

| 用户组 | 权限 | 说明 |
|--------|------|------|
| admin | 全部权限 | 管理员，可管理所有设备 |
| operator | 远程控制 + 文件传输 | 运维人员，可控制分配的设备 |
| viewer | 仅观看 | 只能观看屏幕，不能操作 |
| user | 基础权限 | 普通用户，控制自己的设备 |

**3. 客户端配置**

```bash
# 设置自定义服务器地址
rustdesk --server your-server.com

# 或在配置文件中设置
# RustDesk2.toml
[options]
rendezvous-server = "your-server.com:21116"
relay-server = "your-server.com:21117"
api-server = "https://your-server.com"
```

#### 优势
- ✅ 集中用户管理，支持 LDAP/SSO
- ✅ 精细权限控制
- ✅ 审计日志
- ✅ 设备分组管理
- ✅ 支持地址簿同步

#### 劣势
- ❌ Server Pro 为付费版本
- ❌ 需要额外部署 Web 管理后台

---

### 方案二：开源服务器 + 多实例隔离

#### 架构图

```
┌──────────────────────────────────────────────┐
│              Nginx 反向代理                    │
│        (SSL 终止 / 负载均衡 / 路由)            │
└──────┬──────────┬──────────┬─────────────────┘
       │          │          │
  ┌────▼────┐ ┌──▼─────┐ ┌─▼──────┐
  │ 实例 1   │ │ 实例 2  │ │ 实例 3  │
  │ 部门A    │ │ 部门B   │ │ 部门C   │
  │ :21116  │ │ :22116  │ │ :23116  │
  │ :21117  │ │ :22117  │ │ :23117  │
  └─────────┘ └────────┘ └────────┘
```

#### 部署步骤

**1. 创建多实例 Docker Compose**

```yaml
# docker-compose.yml
version: '3'
services:
  # 部门A实例
  hbbs-dept-a:
    image: rustdesk/rustdesk-server:latest
    command: hbbs
    ports:
      - "21116:21116"
      - "21116:21116/udp"
      - "21115:21115"
    volumes:
      - ./dept-a:/root
    restart: always

  hbbr-dept-a:
    image: rustdesk/rustdesk-server:latest
    command: hbbr
    ports:
      - "21117:21117"
    volumes:
      - ./dept-a:/root
    restart: always

  # 部门B实例
  hbbs-dept-b:
    image: rustdesk/rustdesk-server:latest
    command: hbbs
    ports:
      - "22116:21116"
      - "22116:21116/udp"
      - "22115:21115"
    volumes:
      - ./dept-b:/root
    restart: always

  hbbr-dept-b:
    image: rustdesk/rustdesk-server:latest
    command: hbbr
    ports:
      - "22117:21117"
    volumes:
      - ./dept-b:/root
    restart: always
```

**2. 客户端按部门分配服务器**

```bash
# 部门A客户端
rustdesk --server dept-a.your-server.com

# 部门B客户端
rustdesk --server dept-b.your-server.com
```

#### 优势
- ✅ 完全开源免费
- ✅ 物理隔离，安全性高
- ✅ 各部门独立管理

#### 劣势
- ❌ 资源消耗大（每个实例独立运行）
- ❌ 管理分散
- ❌ 跨部门协作不便

---

### 方案三：单服务器 + 自定义用户认证层

#### 架构图

```
┌────────────┐     ┌──────────────────┐     ┌────────────┐
│  客户端     │────►│  Nginx 代理层     │────►│  hbbs/hbbr │
│            │     │  + 认证中间件      │     │  (原版服务) │
└────────────┘     │  + 用户数据库      │     └────────────┘
                   │  + 权限控制        │
                   └──────────────────┘
```

#### 核心实现

**1. 用户数据库设计**

```sql
CREATE TABLE users (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    username    TEXT NOT NULL UNIQUE,
    password    TEXT NOT NULL,
    group_id    INTEGER REFERENCES groups(id),
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE groups (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        TEXT NOT NULL UNIQUE,
    permissions TEXT DEFAULT 'remote,file,terminal'
);

CREATE TABLE devices (
    id          TEXT PRIMARY KEY,  -- RustDesk 设备ID
    user_id     INTEGER REFERENCES users(id),
    hostname    TEXT,
    platform    TEXT,
    last_online DATETIME
);

CREATE TABLE access_rules (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    from_group  INTEGER REFERENCES groups(id),
    to_group    INTEGER REFERENCES groups(id),
    allowed     BOOLEAN DEFAULT 1
);
```

**2. 客户端定制 - 多用户登录界面**

修改 `src/ui_interface.rs` 增加用户登录逻辑：

```rust
pub fn login(username: String, password: String) -> String {
    let client = reqwest::blocking::Client::new();
    let resp = client.post("https://your-server.com/api/login")
        .json(&serde_json::json!({
            "username": username,
            "password": password
        }))
        .send();
    match resp {
        Ok(r) if r.status().is_success() => {
            let token = r.text().unwrap_or_default();
            Config::set_option("user-token".to_string(), token.clone());
            token
        }
        Ok(r) => format!("登录失败: {}", r.status()),
        Err(e) => format!("连接失败: {}", e)
    }
}
```

**3. 权限控制中间件**

```python
# nginx/auth_middleware.py
def check_access(client_id, target_id):
    client_user = db.get_user_by_device(client_id)
    target_user = db.get_user_by_device(target_id)
    
    if not client_user or not target_user:
        return False
    
    # 同组允许访问
    if client_user.group_id == target_user.group_id:
        return True
    
    # 跨组检查规则
    rule = db.get_access_rule(
        client_user.group_id, 
        target_user.group_id
    )
    return rule and rule.allowed
```

#### 优势
- ✅ 完全开源免费
- ✅ 统一服务器，资源利用率高
- ✅ 自定义认证和权限
- ✅ 灵活的访问控制

#### 劣势
- ❌ 需要开发认证中间件
- ❌ 维护成本较高

---

## 四、方案对比

| 特性 | 方案一 (Server Pro) | 方案二 (多实例) | 方案三 (自定义认证) |
|------|:------------------:|:--------------:|:------------------:|
| 成本 | 💰 付费 | 🆓 免费 | 🆓 免费 |
| 部署难度 | ⭐ 简单 | ⭐⭐ 中等 | ⭐⭐⭐ 复杂 |
| 用户管理 | ✅ 完善 | ⚠️ 分散 | ✅ 自定义 |
| 权限控制 | ✅ 精细 | ✅ 物理隔离 | ✅ 自定义 |
| 审计日志 | ✅ 内置 | ❌ 无 | ⚠️ 需开发 |
| 扩展性 | ✅ 好 | ⚠️ 一般 | ✅ 好 |
| 维护成本 | ⭐ 低 | ⭐⭐ 中 | ⭐⭐⭐ 高 |
| 推荐场景 | 企业生产 | 安全隔离 | 定制需求 |

## 五、推荐实施路径

### 小型团队（< 20 人）→ 方案二

```bash
# 一键部署
git clone https://gitee.com/xueguofang/xiaoxiangyun-ds-remote.git
cd xiaoxiangyun-ds-remote/deploy
docker-compose up -d
```

### 中型团队（20-100 人）→ 方案三

1. 部署开源 hbbs/hbbr
2. 开发认证中间件
3. 定制客户端登录界面
4. 配置权限规则

### 大型团队（> 100 人）→ 方案一

1. 购买 RustDesk Server Pro 许可
2. 集成 LDAP/SSO
3. 配置用户组和权限
4. 启用审计日志

## 六、安全加固建议

### 1. 网络层

```bash
# 仅允许特定 IP 访问服务器
iptables -A INPUT -p tcp --dport 21116 -s 10.0.0.0/8 -j ACCEPT
iptables -A INPUT -p tcp --dport 21116 -j DROP

# 启用 TLS
# 在 Nginx 层配置 SSL 证书
```

### 2. 认证层

- 强制使用永久密码 + 2FA
- 设置密码复杂度要求
- 登录失败锁定策略（代码已内置）

```rust
// src/server/login_failure_check.rs
// 内置了登录失败检测机制
// 可配置最大失败次数和锁定时间
```

### 3. 传输层

- 所有连接端到端加密（基于 NaCl 加密库）
- 中继服务器仅转发加密流量，无法解密
- 支持 TCP/UDP 双协议

### 4. 客户端安全

```bash
# 通过管理策略强制安全配置
# OVERWRITE_SETTINGS (不可被用户修改)
# - confirm-closing-tabs: Y    # 关闭标签需确认
# - enable-confirm-closing-tabs: Y
# - allow-remote-config-modification: N  # 禁止远程修改配置
```

## 七、快速开始

### 最小化部署（5 分钟）

```bash
# 1. 部署服务器
docker run -d --name rustdesk-server \
  -p 21115:21115 -p 21116:21116 -p 21116:21116/udp \
  -p 21117:21117 -p 21118:21118 -p 21119:21119 \
  -v $PWD/data:/root \
  rustdesk/rustdesk-server:latest

# 2. 获取公钥
docker exec rustdesk-server cat /root/id_ed25519.pub

# 3. 客户端配置
# 设置 → 网络 → ID/中继服务器 → 输入服务器IP
# 高级 → Key → 粘贴公钥

# 4. 设置永久密码
# 设置 → 安全 → 设置永久密码

# 5. 远程连接
# 输入对方 ID → 输入密码 → 连接成功
```

### 多用户部署（30 分钟）

```bash
# 使用项目提供的部署脚本
git clone https://gitee.com/xueguofang/xiaoxiangyun-ds-remote.git
cd xiaoxiangyun-ds-remote/deploy
./deploy-multi-user.sh
```

---

> 📖 仓库地址: https://gitee.com/xueguofang/xiaoxiangyun-ds-remote
> 
> 📝 版本: v1.4.6
> 
> 👤 维护: xueguofang
